# Kết quả Benchmark - Slot 15 07/07/2026 (STT21 Verification Run #3)

- **Cấu hình**: Baseline mới (STT21: `--enable-chunked-prefill` + `--no-enable-log-requests` + `--quantization=fp8` + `--gpu-memory-utilization=0.95` + `--enable-prefix-caching`).
- **Mục đích**: Chạy kiểm thử lặp lại cấu hình tốt nhất hiện tại (STT21 - 18.99đ) để tính trung bình và loại bỏ sai số do hệ thống của ban tổ chức.

## Chỉ số đo được

TBD

---
