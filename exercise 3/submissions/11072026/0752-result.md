# Kết quả Benchmark - 07:52 11/07/2026 (STT 78 - FlashInfer Backend + BF16 gốc, seqs 64 🔥)

- **Cấu hình**: Image `ptquanh/sandbox-runtime:vllm-v0.22.1-flashinfer` + `--max-num-seqs 64` + BF16 gốc
- **Mục đích**: Khảo sát hiệu năng cận dưới của batch size (`--max-num-seqs=64`) trên FlashInfer backend. Nhằm so sánh xem batch size nhỏ có làm nghẽn hàng đợi (TTFT vọt) hay không.

## Chỉ số đo được

**Đang chờ kết quả benchmark (TBD)**
