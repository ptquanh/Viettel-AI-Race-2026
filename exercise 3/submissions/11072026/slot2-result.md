# Kết quả Benchmark - 14:45 11/07/2026 (STT 87 - FP8 weights + Chunk 4096 + Seqs 512)

- **Cấu hình**: Image `vllm/vllm-openai:v0.22.1` + Chunked Prefill 4096 + `max-num-seqs=512`.
- **Mục đích**: Tăng mức độ xử lý song song để ép tải hệ thống, kết hợp với chunked prefill để xem có tối ưu được TPOT hay không.

## Chỉ số đo được

**Đang chờ kết quả benchmark (TBD)**
