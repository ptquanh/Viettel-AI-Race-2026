# Kết quả Benchmark - 15:00 11/07/2026 (STT 88 - FP8 + Chunk 4096 + disable-async-output)

- **Cấu hình**: Image `vllm/vllm-openai:v0.22.1` + Chunked Prefill 4096 + `--disable-async-output-proc`.
- **Mục đích**: Giảm tải cho CPU (chỉ có 3 core) bằng cách tắt tiến trình xử lý đầu ra bất đồng bộ.

## Chỉ số đo được

**Đang chờ kết quả benchmark (TBD)**
