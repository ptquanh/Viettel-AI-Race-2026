# Kế hoạch Thực thi (Phục thù) — Ngày 14/07/2026

Dựa trên sự cố quên bật biến môi trường ở ngày 12/07, ngày 14/07 hoàn toàn tập trung vào việc **nộp lại (resubmit)** các cấu hình đã được thiết kế kỹ lưỡng của The 100-Point Formula, nhưng lần này với Custom Triton Kernel được kích hoạt đầy đủ để triệt tiêu Queuing Delay.

## 1. Mục tiêu cốt lõi

- **Kích hoạt Monkey Patch**: Bắt buộc phải truyền `VLLM_CUSTOM_KERNEL=1` vào tất cả các compose file để vô hiệu hóa FP8 Dequantization gốc của vLLM và kích hoạt Custom Triton Kernel.
- **Khắc phục lỗi YAML**: Đảm bảo GPU configuration ở định dạng nested mapping chính xác thay vì yaml array list để tránh lỗi portal không nhận diện được cờ `nvidia`.

## 2. Kết quả các Slot đã nộp trong ngày (14/07)

### Slot 1 (0820): Warmup Ablation — Score: 2.24

- **Cấu hình**: Seqs=32, Warmup=OFF, Chunked Prefill=OFF, Custom Kernel=ON.
- **Kết quả**: TPOT 56ms, TTFT P50 3773ms. Thất bại hoàn toàn.
- **Nguyên nhân**: Tắt cả Warmup lẫn Chunked Prefill → GPU bị khóa cứng trong prefill tuần tự 20k tokens, chặn toàn bộ decode step.

### Slot 2 (0856): Warmup + Custom Kernel — Score: 2.21

- **Cấu hình**: Seqs=32, Warmup=ON, Chunked Prefill=OFF, Custom Kernel=ON.
- **Kết quả**: TPOT 57ms, TTFT P50 3730ms. Vẫn thất bại.
- **Bài học**: Warmup + Custom Kernel KHÔNG giải quyết được TPOT nếu Chunked Prefill bị tắt. Khi tắt chunked prefill, các user query lớn (10-20k tokens) chặn hoàn toàn decode.

### Slot 3 (1058): + Chunked Prefill ON — Score: 39.83 🏆 KỶ LỤC MỚI

- **Cấu hình**: Seqs=24, Warmup=ON, Chunked Prefill=ON (chunk 4096), Custom Kernel=ON.
- **Kết quả**: TPOT **22ms**, TTFT P50 **3177ms**. Passed SLO: 32/120.
- **Phân tích**: TPOT xuất sắc (gần Floor 20ms). Tuy nhiên TTFT 3177ms >> Ceiling 1500ms → mọi request nhận **0 điểm TTFT**. Toàn bộ 39.83 điểm chỉ đến từ thành phần TPOT.

### Slot 4 (1138): Seqs 256 Concurrency Test — Score: 23.98

- **Cấu hình**: Seqs=256, Warmup=ON, Chunked Prefill=ON (chunk 4096), Custom Kernel=ON.
- **Kết quả**: TPOT **30ms**, TTFT P50 **1764ms**, TTFT P95 **2585ms**. Passed SLO: 91/120.
- **Phân tích**: Đúng như dự đoán, việc tăng concurrency lên 256 giúp giải phóng hàng đợi, kéo TTFT P50 giảm sâu từ 3.17s xuống còn 1.76s (gần chạm trần 1.5s) và TTFT P95 giảm hơn 3s xuống 2.58s. Nhờ đó, số request passed SLO tăng vọt từ 32 lên 91! Tuy nhiên, do TTFT P50 vẫn > 1.5s nên tiếp tục bị 0 điểm TTFT. Đồng thời, TPOT tăng nhẹ từ 22ms lên 30ms làm giảm điểm thành phần TPOT, dẫn đến tổng điểm giảm từ 39.83 xuống 23.98.

### Slot 5 (1348): Seqs 128 Concurrency Test — Score: 20.64

- **Cấu hình**: Seqs=128, Warmup=ON, Chunked Prefill=ON (chunk 4096), Custom Kernel=ON.
- **Kết quả**: TPOT **31ms**, TTFT P50 **1438ms**, TTFT P95 **2745ms**. Passed SLO: 95/120.
- **Phân tích**: Giảm concurrency xuống 128 giúp TTFT P50 lần đầu tiên chui xuống dưới trần 1500ms (đạt 1438ms) và passed_slo tăng lên 95/120. Tuy nhiên, do TTFT vẫn quá sát trần (được cực ít điểm do hàm phạt bình phương), cộng với việc TPOT tăng nhẹ lên 31ms (so với 22ms ở Seqs 24), tổng điểm giảm còn 20.64.

### Slot 6 (1353): Seqs 64 Concurrency Test — Score: 20.57

- **Cấu hình**: Seqs=64, Warmup=ON, Chunked Prefill=ON (chunk 4096), Custom Kernel=ON.
- **Kết quả**: TPOT **30ms**, TTFT P50 **1636ms**, TTFT P95 **2884ms**. Passed SLO: 74/120.
- **Phân tích**: Giảm concurrency xuống 64 làm tăng TTFT P50 lên 1636ms (vượt trần 1.5s) và giảm passed_slo xuống 74/120 do nghẽn hàng đợi. TPOT đạt 30ms, tương đương mức điểm 20.57.

## 3. Phân tích Trace & Công thức Tính điểm

### Cấu trúc Trace (Phát hiện quan trọng)

```
120 requests = 20 đoạn hội thoại × 6 lượt (turns)
Mỗi 5 giây → 20 requests ập vào đồng thời (25ms/request)

t=0s:  Turn 1 — 2 msgs  — ~20k tokens/req (System + User Q1)
t=5s:  Turn 2 — 4 msgs  — ~24k tokens/req (+ Asst A1 + User Q2)
t=10s: Turn 3 — 6 msgs  — ~28k tokens/req
t=15s: Turn 4 — 8 msgs  — ~33k tokens/req
t=20s: Turn 5 — 10 msgs — ~37k tokens/req
t=25s: Turn 6 — 12 msgs — ~42k tokens/req

Tất cả 20 hội thoại chia sẻ cùng 1 System Prompt (~20k tokens).
Lượt trước là tiền tố (prefix) của lượt sau → Prefix Cache Hit hoàn hảo.
```

### Công thức tính điểm (Bảng tham số)

| Tham số | Giá trị | Ý nghĩa                            |
| ------- | ------- | ---------------------------------- |
| F_ttft  | 100ms   | TTFT ≤ 100ms → điểm tối đa         |
| C_ttft  | 1500ms  | TTFT ≥ 1500ms → 0 điểm             |
| F_tpot  | 20ms    | TPOT ≤ 20ms → điểm tối đa          |
| C_tpot  | 45ms    | TPOT ≥ 45ms → 0 điểm               |
| γ       | 2       | Phạt bậc hai (quadratic)           |
| w       | 0.5     | TTFT và TPOT có trọng số bằng nhau |

### Nút thắt cốt lõi: TTFT

- Với 1058: TTFT=3177ms → `s_ttft = 0` cho MỌI request → 50% điểm bị mất hoàn toàn.
- TPOT=22ms → `s_tpot = ((45-22)/(45-20))^2 = (23/25)^2 = 0.8464` → Gần tối đa.
- **Kết luận**: Muốn đạt >60 điểm, BẮT BUỘC phải kéo TTFT xuống dưới 1500ms.

### Tại sao TTFT cao ở Seqs=24?

- STT 89 (Seqs=256): TTFT P50 = **1903ms**, passed SLO = 62/120.
- STT 94 (Seqs=24): TTFT P50 = **3177ms**, passed SLO = 32/120.
- Khi 20 requests ập vào cùng lúc, Seqs=24 chỉ còn ~4 slot trống (nếu requests trước vẫn đang decode) → 16 requests bị xếp hàng chờ slot → TTFT bị đội lên gấp đôi.
- **Giải pháp**: Nâng `max-num-seqs` lên đủ cao để admit ngay lập tức toàn bộ burst 20 requests.

## 4. Kế hoạch 12 Slot còn lại (Buổi chiều)

### Cấu hình cố định (Baseline cho mọi slot)

```yaml
image: ptquanh/sandbox-runtime:vllm-phase2-fp8-warmup-v2
environment:
  - VLLM_QUANTIZATION=fp8
  - VLLM_KV_CACHE_DTYPE=fp8
  - VLLM_WARMUP=1
  - VLLM_ENABLE_CHUNKED_PREFILL=1
  - VLLM_CUSTOM_KERNEL=1
```

### Phase 1: Seqs Sweep (5 slots) — Chunk=4096, OMP=3

> Mục tiêu: Tìm giá trị `max-num-seqs` tối ưu cân bằng TTFT và TPOT.

| Slot | Tên file | Seqs | Chunk | Kết quả / Giả thuyết                                                                                          |
| ---- | -------- | ---- | ----- | ------------------------------------------------------------------------------------------------------------- |
| 4    | `1138`   | 256  | 4096  | **Đã nộp (23.98 điểm).** TTFT P50 1764ms (gần đạt điểm), passed SLO vọt lên 91/120, nhưng TPOT tăng lên 30ms. |
| 5    | `1348`   | 128  | 4096  | **Đã nộp (20.64 điểm).** TTFT P50 1438ms (lần đầu dưới 1.5s), passed SLO 95/120, nhưng TPOT tăng lên 31ms.    |
| 6    | `1353`   | 64   | 4096  | **Đã nộp (20.57 điểm).** TTFT P50 1636ms (vượt trần 1.5s), passed SLO 74/120, TPOT 30ms.                      |
| 7    | `1409`   | 48   | 4096  | **Đang nộp.** Giới hạn gần burst size (20 req). Nếu burst trước xong kịp, TTFT sẽ tốt.                        |
| 8    | `slot08` | 32   | 4096  | So sánh trực tiếp với 1058 (Seqs=24). Xem thêm 8 slot có giảm TTFT không.                                     |

### Phase 2: Chunk Size Sweep (3 slots) — Dùng Seqs tốt nhất từ Phase 1

> Mục tiêu: Tối ưu kích thước chunk prefill.

| Slot | Tên file | Seqs | Chunk | Giả thuyết                                                                                      |
| ---- | -------- | ---- | ----- | ----------------------------------------------------------------------------------------------- |
| 9    | `slot09` | best | 2048  | Chunk nhỏ hơn → ít gián đoạn decode hơn → TPOT giảm, nhưng prefill chậm hơn → TTFT tăng.        |
| 10   | `slot10` | best | 8192  | Chunk lớn hơn → prefill nhanh hơn → TTFT giảm, nhưng decode bị gián đoạn nhiều hơn → TPOT tăng. |
| 11   | `slot11` | best | 16384 | Chunk rất lớn. Nếu mô hình nhỏ (2B), overhead per-chunk-step thấp → có thể tối ưu cho TTFT.     |

### Phase 3: Fine-tuning (4 slots) — Tinh chỉnh dựa trên kết quả Phase 1+2

> Mục tiêu: Tối ưu các tham số phụ.

| Slot | Tên file | Thay đổi                                       | Giả thuyết                                                                                    |
| ---- | -------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 12   | `slot12` | OMP=2                                          | Giảm CPU threads từ 3→2 để giảm context switching overhead trên 3-core MiG.                   |
| 13   | `slot13` | OMP=4                                          | Tăng CPU threads lên 4. Mặc dù MiG chỉ có 3 cores, hyperthreading có thể giúp.                |
| 14   | `slot14` | Warmup=OFF + best params                       | Đánh giá tác động warmup khi đã bật Chunked Prefill + Custom Kernel.                          |
| 15   | `slot15` | Combo tốt nhất + `VLLM_DISABLE_ASYNC_OUTPUT=1` | Tắt async output processing. Giảm overhead thread coordination, có thể giúp TPOT trên 3-core. |

## 5. Checklist

### Đã hoàn thành

- [x] Đã khôi phục file kết quả của 12/07 về trạng thái lưu trữ nguyên bản.
- [x] Đã tạo và nộp `0820`, `0856`, `1058` trong `14072026/`.
- [x] Đã kiểm tra chéo: tất cả file đều có `- VLLM_CUSTOM_KERNEL=1`.
- [x] Đã cập nhật kết quả 4 slot (`0820`, `0856`, `1058`, `1138`) vào `logs.md` và bảng kỷ lục.
- [x] Đã phân tích sâu cấu trúc trace và công thức tính điểm.
- [x] Đã tạo tất cả 12 file docker-compose cho Phase 1-3.
- [x] Đã nộp Slot 4 (`1138`) và ghi nhận kết quả.

### Cần thực hiện

- [/] Nộp tiếp Phase 1 (4 slots còn lại: `slot05` đến `slot08`) và ghi nhận kết quả.
- [ ] Dựa trên Phase 1, xác định Seqs tối ưu nhất và tạo/sửa file Phase 2.
- [ ] Nộp Phase 2 (3 slots) và ghi nhận kết quả.
- [ ] Dựa trên Phase 1+2, tạo/sửa file Phase 3 fine-tuning.
- [ ] Nộp Phase 3 (4 slots) và ghi nhận kết quả.
- [ ] Cập nhật `logs.md` sau mỗi phase.
