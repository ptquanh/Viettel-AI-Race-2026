# Kế hoạch Thực thi (Phục thù) — Ngày 14/07/2026

Dựa trên sự cố quên bật biến môi trường ở ngày 12/07, ngày 14/07 hoàn toàn tập trung vào việc **nộp lại (resubmit)** các cấu hình đã được thiết kế kỹ lưỡng của The 100-Point Formula, nhưng lần này với Custom Triton Kernel được kích hoạt đầy đủ để triệt tiêu Queuing Delay.

## 1. Mục tiêu cốt lõi
- **Kích hoạt Monkey Patch**: Bắt buộc phải truyền `VLLM_CUSTOM_KERNEL=1` vào tất cả các compose file để vô hiệu hóa FP8 Dequantization gốc của vLLM và kích hoạt Custom Triton Kernel.
- **Khắc phục lỗi YAML**: Đảm bảo GPU configuration ở định dạng nested mapping chính xác thay vì yaml array list để tránh lỗi portal không nhận diện được cờ `nvidia`.

## 2. Kế hoạch Triển khai các Slot ngày 14/07

### Slot 1: Warmup Ablation (Bản gốc là Slot 3 của 12/07)
- **Cấu hình**: `FP8 Weights` + `Custom FP8 KV Cache` + `Seqs = 32` + **Không Warmup** (`VLLM_WARMUP=0`) + `Custom Kernel ON`.
- **Đặc điểm**: Tắt tính năng Warmup JIT.
- **Mục tiêu**: Làm bản đối chứng. Đánh giá chính xác tác động của Monkey Patch lên TTFT/TPOT khi không có hệ thống Prefix Warmup hỗ trợ.

### Slot 2: Nộp lại cấu hình 0747 (The Ultimate 100-Point Formula)
- **Cấu hình**: `FP8 Weights` + `Custom FP8 KV Cache` + `Seqs = 32` + **Có Warmup** (`VLLM_WARMUP=1`) + `Custom Kernel ON`.
- **Mục tiêu**: Chạy lại Slot 1 ngày 12/07. Cấu hình lý tưởng nhất để TPOT giảm xuống $\le 20\text{ms}$ và triệt tiêu TTFT P50.

### Slot 3: Nộp lại cấu hình 0812 (Concurrency Tweak)
- **Cấu hình**: Giống hệt Slot 2, nhưng siết chặt `max-num-seqs` xuống **24**.
- **Mục tiêu**: Chạy lại Slot 2 ngày 12/07. Giới hạn concurrency vừa khớp với burst 20 requests/5s của Trace dataset, ngăn vLLM prefill dư thừa và tối ưu hóa TPOT xuống ngưỡng vật lý thấp nhất.

## 3. Checklist đã hoàn thành
- [x] Đã khôi phục file kết quả của 12/07 về trạng thái lưu trữ nguyên bản.
- [x] Đã tạo `slot1-docker-compose.yml`, `slot2-docker-compose.yml`, `slot3-docker-compose.yml` trong `14072026/`.
- [x] Đã kiểm tra chéo: tất cả 3 file đều có `- VLLM_CUSTOM_KERNEL=1`.
- [x] Đã kiểm tra chéo: thẻ `devices` được cấu hình dạng lồng nhau (nested dict) chính xác.

Sẵn sàng nộp bài trên Portal!
