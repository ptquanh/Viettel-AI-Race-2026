# Kế hoạch tối ưu hóa & nộp bài - Ngày 16/07/2026 (Plan-1607)

Kế hoạch này khởi động quá trình tối ưu hóa cho **Đề bài mới của BTC** sử dụng mô hình **LiquidAI/LFM2.5-1.2B-Instruct** trên hạ tầng MiG H200 (18GB VRAM, 3 Core CPU, 8GB RAM).

Mục tiêu chính trong ngày 16/07 là thiết lập baseline chuẩn, đo lường các chỉ số latency thực tế trên trace Poisson mới (`trace_grading_public.jsonl`), và bắt đầu tinh chỉnh để đưa TPOT về sát mức Floor 1ms và TTFT dưới 400ms.

---

## 1. Phân tích Cực trị Latency mới

Theo quy chế mới:

- **TTFT**: Floor = **10 ms**, Ceiling = **400 ms**, Trọng số $w = 0.5$
- **TPOT**: Floor = **1 ms**, Ceiling = **10 ms**, Trọng số $1-w = 0.5$

Do LFM2.5-1.2B là kiến trúc Recurrent State-Space Model, tốc độ sinh của nó cực kỳ nhanh và không bị trễ lũy tiến theo chiều dài context. Cổ chai chính hiện tại 100% đến từ **CPU scheduling overhead** và **Prefill Interference**.

---

## 2. KẾ HOẠCH PHASE 1: THĂM DÒ & XÁC NHẬN MÔI TRƯỜNG LFM2.5

**Mục tiêu:** Xác nhận hệ thống chấm điểm đã chuyển sang LFM2.5 và thu thập mốc baseline đầu tiên.

```yaml
services:
  model:
    image: vllm/vllm-openai:v0.22.1
    entrypoint:
      - python3 #Don't change this to vllm-server
      - -m #Don't change this to vllm-server
      - vllm.entrypoints.openai.api_server #Don't change this to vllm-server
    command:
      - --model=/model #Don't change this to vllm-server
      - --served-model-name=LFM2.5-1.2B-Instruct #Don't change this to vllm-server
      - --host=0.0.0.0 #Don't change this to vllm-server
      - --port=8000 #Don't change this to vllm-server
      - --max-model-len=32768
      - --gpu-memory-utilization=0.95
      - --tensor-parallel-size=1
      - --enable-prefix-caching
    ports:
      - "8000:8000"
    shm_size: "2g"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
```

### Các Slot triển khai Phase 1:

| Tên Slot | Tên file compose          | Cấu hình thay đổi                                                                                                  | Mục tiêu / Kỳ vọng                                                                                                             |
| :------: | :------------------------ | :----------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------- |
| **1333** | `1333-docker-compose.yml` | Base Slot + env `OMP_NUM_THREADS=4` + cmd `--max-num-seqs=48` + `--no-enable-log-requests` + `--disable-log-stats` | **Đã nộp (42.91 điểm - STT 118).** Xác nhận chạy thành công LFM2.5. TTFT P95 = 151ms (cực tốt), nhưng TPOT = 5ms (cần tối ưu). |

**Kết luận Phase 1:** Mô hình chạy rất nhanh, VRAM dư dả. Chúng ta cần tập trung toàn lực vào việc giải nén CPU Overhead để ép TPOT từ 5ms xuống < 3ms.

---

## 3. KẾ HOẠCH PHASE 2: ÉP CHẶT CONCURRENCY (SEQS TUNING) GIẢI NÉN TPOT

**Mục tiêu:** Hạ `max-num-seqs` để giảm số lượng request vLLM scheduler phải duyệt mỗi bước, qua đó giảm CPU overhead và kéo TPOT xuống thấp nhất có thể.
**Chiến lược nộp bài:** Các slot trong Phase này hoàn toàn độc lập về cấu hình, có thể **nộp song song** liên tiếp để tiết kiệm thời gian chờ đợi grader.

### Các Slot triển khai Phase 2:

|  Tên Slot  | Tên file compose           | Cấu hình thay đổi                                                                                                  | Mục tiêu / Kỳ vọng                                                                                    |
| :--------: | :------------------------- | :----------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------- |
|  **1645**  | `1645-docker-compose.yml`  | Base Slot + env `OMP_NUM_THREADS=4` + cmd `--max-num-seqs=32` + `--no-enable-log-requests` + `--disable-log-stats` | **Sẵn sàng nộp.** Ép nhẹ concurrency, kỳ vọng TPOT giảm còn 3-4ms.                                    |
| **Slot 3** | `slot3-docker-compose.yml` | Base Slot + env `OMP_NUM_THREADS=4` + cmd `--max-num-seqs=24` + `--no-enable-log-requests` + `--disable-log-stats` | **Sẵn sàng nộp.** Ép vừa phải concurrency, kỳ vọng TPOT giảm còn 2-3ms.                               |
| **Slot 4** | `slot4-docker-compose.yml` | Base Slot + env `OMP_NUM_THREADS=4` + cmd `--max-num-seqs=16` + `--no-enable-log-requests` + `--disable-log-stats` | **Sẵn sàng nộp.** Ép chặt cực độ để xem TPOT có chạm sàn < 2ms hay không (đánh đổi TTFT có thể tăng). |

---

## 4. KẾ HOẠCH PHASE 3: TỐI ƯU HÓA LẬP LỊCH & LUỒNG CPU (PREFILL INTERFERENCE)

**Mục tiêu:** Xử lý hiện tượng nghẽn luồng decode khi có các request mới mang theo hàng ngàn token context (Prefill Interference), đồng thời khớp luồng CPU với giới hạn vật lý của Node.
**Chiến lược nộp bài:** Các slot này bắt buộc phải nộp sau khi Phase 2 hoàn thành. Chúng ta sẽ **kế thừa giá trị `VLLM_MAX_NUM_SEQS` tốt nhất** từ Phase 2 (gọi là `BEST_SEQS`) làm nền tảng.

### Các Slot triển khai Phase 3:

|  Tên Slot  | Tên file compose           | Cấu hình thay đổi                                                              | Mục tiêu / Kỳ vọng                                                                                                         |
| :--------: | :------------------------- | :----------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------- |
| **Slot 5** | `slot5-docker-compose.yml` | `BEST_SEQS` + cmd `--enable-chunked-prefill` + `--max-num-batched-tokens=1024` | Cắt nhỏ 4K token prefill thành các chunk 1024, cho phép interleave prefill và decode, giữ TPOT P99 ổn định không bị spike. |
| **Slot 6** | `slot6-docker-compose.yml` | `BEST_SEQS` + env `OMP_NUM_THREADS=2` (hoặc 3)                                 | Tinh chỉnh luồng OpenMP khớp chính xác với 3 core vật lý của MiG H, triệt tiêu context switching overhead.                 |

---

## 5. KẾ HOẠCH PHASE 4: TỐI ĐA THÔNG LƯỢNG BỘ NHỚ (ADVANCED TECHNIQUES)

**Mục tiêu:** Khai thác các tính năng phần cứng và lượng tử hóa để tăng tốc độ tính toán ma trận.
**Chiến lược nộp bài:** Kế thừa cấu hình hoàn hảo nhất sau Phase 3.

### Các Slot triển khai Phase 4:

|  Tên Slot  | Tên file compose           | Cấu hình thay đổi                                    | Mục tiêu / Kỳ vọng                                                                                                           |
| :--------: | :------------------------- | :--------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------- |
| **Slot 7** | `slot7-docker-compose.yml` | Cấu hình tốt nhất Phase 3 + cmd `--quantization=fp8` | Lượng tử hóa trọng số online (nếu image vLLM hiện tại hỗ trợ) để giảm băng thông bộ nhớ, kỳ vọng tăng gấp đôi tốc độ decode. |

---

## 6. Checklist Tiến độ Ngày 16/07/2026

**Phase 1 & Phase 2:**

- [x] Soạn thảo và cấu hình các file docker-compose (1333, 1645).
- [x] Nộp Slot 1333 (Phase 1) và đánh giá -> **Đã chạy thử (42.91 điểm).**
- [ ] Nộp Slot 1645 (Seqs=32) và đánh giá.
- [ ] Thiết kế file compose cho Slot 3 (Seqs=24) và Slot 4 (Seqs=16).
- [ ] Nộp song song Slot 3 và Slot 4.

**Phase 3 & Phase 4:**

- [ ] Xác định `BEST_SEQS` từ kết quả Phase 2.
- [ ] Thiết kế file compose cho Slot 5 (Chunked Prefill) và Slot 6 (OMP Threads).
- [ ] Nộp Slot 5, 6 và đánh giá.
- [ ] Thiết kế và nộp Slot 7 (FP8 Quantization) nếu khả thi.
