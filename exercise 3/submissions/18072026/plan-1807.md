# Kế hoạch tối ưu hóa LFM2.5 - Ngày 18/07/2026

## 0. MỤC TIÊU NGÀY 18/07: ĐÁNH GIÁ CUSTOM DOCKER IMAGE & CHUẨN BỊ NỀN TẢNG CHO 80+

> **Mục tiêu ngày 18/07**: Đạt **60-68 điểm** bằng việc nộp Custom Docker Image tích hợp Triton Kernels & JIT Warmup ngay từ đầu, bỏ qua việc test thêm stock image.
>
> **Mục tiêu dài hạn**: 80+ điểm (xem [Lộ trình tổng thể](../../docs/phase1_execution_plan.md) — Giai đoạn 3-4, dự kiến 23-30/07).

### Phân tích khoảng cách (Gap Analysis)

**Công thức chấm điểm:**

```
S_request = 0.5 × s_ttft + 0.5 × s_tpot
s_ttft = clamp((400 - TTFT) / 390, 0, 1)²
s_tpot = clamp((10 - TPOT) / 9, 0, 1)²
```

**Hiện trạng tốt nhất (STT 16 ngày 17/07 — 58.94 điểm):**

| Chỉ số    | Giá trị hiện tại | s_score hiện tại | Giá trị cần cho 80+ | s_score cần |
| :-------- | :--------------: | :--------------: | :-----------------: | :---------: |
| TPOT (ms) |        4         |    **0.444**     |         ≤ 2         |  **0.790**  |
| TTFT (ms) |     76 (P95)     |    **0.690**     |        ≤ 50         |  **0.805**  |
| **ERS**   |                  |    **0.589**     |                     |  **0.798**  |

> [!IMPORTANT]
> **Khoảng cách từ 58.94 → 80 là lớn** và cần nhiều giai đoạn phát triển. Ngày 18/07 tập trung **Giai đoạn 2** (Custom Docker Image Tuning), kỳ vọng đạt 60-68 điểm.

### Bảng mục tiêu theo kịch bản ngày 18/07

| Kịch bản                                | TPOT  | s_tpot | TTFT P95 | s_ttft |  ERS  |   Điểm    | Khả thi?      |
| :-------------------------------------- | :---: | :----: | :------: | :----: | :---: | :-------: | :------------ |
| Hiện tại (FP8 stock image - STT 16)     |  4ms  | 0.444  |   76ms   | 0.690  | 0.589 | **58.94** | ✅ Đã đạt     |
| Custom Image (Warmup + Kernel baseline) | 3.5ms | 0.520  |   60ms   | 0.760  | 0.640 | **64.00** | ⚠️ Đang chạy  |
| Custom Image + Optimized Triton Kernels | 3.0ms | 0.605  |   55ms   | 0.782  | 0.694 | **69.35** | ⚠️ Thử nghiệm |

---

## 1. PHÂN TÍCH CỔ CHAI LFM2.5-1.2B-Instruct

### Kiến trúc LFM2.5 vs Transformer

LFM2.5 là mô hình **lai Recurrent + Attention** (Liquid Foundation Model):

- **Recurrent layers** (chiếm đa số): Nén context thành trạng thái cố định → O(1) memory per decode step, KHÔNG đọc KV cache.
- **Attention layers** (thiểu số, GQA): Vẫn dùng KV cache nhưng rất ít layers → bandwidth thấp hơn nhiều so với Transformer thuần.

### Breakdown TPOT = 4ms (FP8 Weights, Batch ≈ 32 sequences)

| Thành phần                          | Thời gian ước tính | Tỉ lệ |
| :---------------------------------- | :----------------: | :---: |
| **Weight reads (FP8, 1.2GB)**       |      ~1.75 ms      |  44%  |
| **KV cache reads (attention)**      |      ~0.60 ms      |  15%  |
| **Recurrent state update**          |      ~0.40 ms      |  10%  |
| **Computation (MatMul + Act)**      |      ~0.50 ms      |  13%  |
| **Overhead (kernel launch, sched)** |      ~0.75 ms      |  18%  |
| **Tổng**                            |    **~4.00 ms**    | 100%  |

> [!IMPORTANT]
> **Phát hiện then chốt**: Weight reads chiếm **44%** thời gian TPOT. Đây là đòn bẩy lớn nhất. Giảm kích thước weights từ FP8 (1 byte) xuống INT4 (0.5 byte) sẽ giảm ~0.87ms, kéo TPOT xuống ~3.1ms. Kết hợp CUDA Graphs giảm overhead kernel launch, tiềm năng đạt ~2.5ms.

### Breakdown TTFT = 76ms (FP8, OMP=2)

| Thành phần | Thời gian ước tính |
| :-## 2. BA TRACK TỐI ƯU HÓA

### Track A: Custom Docker Image + Custom Kernels & Online FP8 ⭐ (ƯU TIÊN #1)

**Mục tiêu**: Giảm TPOT từ 4ms → ~2.8ms, TTFT từ 76ms → ~55ms.
**Kỳ vọng điểm**: 60-70 điểm.

> [!IMPORTANT]
> **Tuân thủ quy chế chống gian lận (Mục 5):** Thí sinh không được tự ý can thiệp tokenizer hoặc thay đổi weights offline (không dùng AWQ/GPTQ offline weights). Thư mục `/model` chứa weights gốc là cố định.
> Do đó, Track A tập trung vào việc dùng Custom Docker Image để **tiêm (inject) custom Triton/CUDA kernels** tăng tốc tính toán trực tiếp trên weights gốc và KV Cache được lượng tử hóa online.

#### Bước 1: Viết và tối ưu hóa Custom Triton/CUDA Kernels cho LFM recurrent layers

Viết custom kernels tối ưu hóa các phép tính tuần hoàn (gated short-convolution blocks) của LFM2.5 và fusion layers (như LayerNorm + Linear + Activation) để chạy trực tiếp trên GPU.

#### Bước 2: Build Custom Docker Image (tái sử dụng hạ tầng Round 1)

```dockerfile
FROM vllm/vllm-openai:v0.22.1

# Cài đặt các custom kernels đã compiled
COPY ./custom_kernels /app/custom_kernels
RUN cd /app/custom_kernels && pip install .

# Setup hijack script để tiêm custom kernels lúc start
COPY ./hijack /app/hijack
ENV VLLM_CUSTOM_KERNEL=1
```

```bash
# Build và push image lên Docker Hub
docker build -t ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1 .
docker push ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1
```

#### Bước 3: Docker Compose chạy với Custom Image & Online FP8

```yaml
services:
  model:
    image: ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1
    environment:
      - OMP_NUM_THREADS=2
    entrypoint:
      - python3
      - -m
      - vllm.entrypoints.openai.api_server
    command:
      - --model=/model # Giữ nguyên model gốc theo đúng quy định BTC
      - --served-model-name=LFM2.5-1.2B-Instruct
      - --host=0.0.0.0
      - --port=8000
      - --max-model-len=8192
      - --gpu-memory-utilization=0.95
      - --tensor-parallel-size=1
      - --enable-prefix-caching
      - --quantization=fp8 # Sử dụng online quantization hợp lệ của vLLM
      - --no-enable-log-requests
      - --disable-log-stats
      - --max-num-seqs=32
      - --enable-chunked-prefill
      - --max-num-batched-tokens=4096
    ports:
      - "8000:8000"
    shm_size: "2g"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
```

> [!WARNING]
> **Rủi ro chính của Track A:**
>
> 1. Thiết kế recurrent layers của LFM2.5 rất đặc thù, việc fused kernel tự viết đòi hỏi kiểm thử kỹ để không bị sai lệch accuracy (hậu kiểm GPQA).
> 2. JIT overhead của custom kernels khi khởi chạy lần đầu có thể làm tăng TTFT của request đầu tiên (cần warmup cẩn thận).

#### Xác minh và Tuân thủ Luật chơi

Mọi submission sẽ chạy hoàn toàn trên `/model` mặc định do BTC mount, sử dụng các tham số tối ưu hóa online được vLLM hỗ trợ. Cách này hoàn toàn tuân thủ các quy định chống gian lận.

---

## 2. KẾ HOẠCH THỬ NGHIỆM 15 SLOTS TRÊN NỀN CUSTOM IMAGE

> [!IMPORTANT]
> Toàn bộ 15 slots của ngày 18/07 sẽ chạy **100% trên nền Custom Docker Image** `ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1` để thừa hưởng cơ chế Warmup JIT (loại bỏ trễ khởi chạy) và tối ưu hóa Triton kernels.
> Cấu hình nền tảng kế thừa từ **STT 16 ngày 17/07 (58.94 điểm)** kết hợp với các tối ưu hóa của Custom Image.

> [!NOTE]
> **Sự khác biệt của Custom Image so với Stock Image:**
>
> 1. **Warmup JIT**: Thực hiện chạy thử một số request ảo lúc startup để ép GPU khởi tạo và compile JIT các kernel tuần hoàn trước khi Grader tiến hành đo đạc, triệt tiêu hoàn toàn trễ khởi động (giảm TTFT của lượt request đầu tiên).
> 2. **Triton Custom Kernels**: Tích hợp các nhân CUDA/Triton tự biên dịch đặc thù cho recurrent layers của LFM2.5 để tối ưu hóa weight reads (chiếm 44% TPOT) và giảm overhead scheduling.

#### Nhóm 1: Custom Image Baseline & Compilation (4 slots)

_Mục tiêu: Đo lường hiệu năng của Custom Image đơn thuần so với Stock Image, đồng thời đánh giá hiệu quả của torch.compile trên nền image này._

| Slot | Cấu hình thay đổi so với Baseline Custom Image | Mục tiêu                                                                                                                                                                     |
| :--: | :--------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|  1   | Không thay đổi (Baseline Custom Image)         | So sánh Custom Image (có Warmup JIT + Custom Kernel hook) với Stock Baseline cao điểm nhất hiện tại (**STT 16 ngày 17/07: 58.94 điểm**) chạy cùng tham số (OMP=2, FP8 Base). |
|  2   | + `--compilation-config '{"level": 3}'`        | Bật tối ưu hóa torch.compile aggressive (operator fusion + CUDA graph nâng cao).                                                                                             |
|  3   | + `--compilation-config '{"level": 2}'`        | So sánh với level 3 để chọn mức compile an toàn nhất.                                                                                                                        |
|  4   | + `--enforce-eager`                            | Tắt hoàn toàn CUDA Graphs để đo baseline thuần eager trên custom image.                                                                                                      |

#### Nhóm 2: Scheduler & Batching Tuning (3 slots)

_Mục tiêu: Tìm kiếm điểm ngọt (sweet-spot) của chunk size và block size trên custom image._

| Slot | Cấu hình thay đổi so với Baseline Custom Image | Mục tiêu                                                                   |
| :--: | :--------------------------------------------- | :------------------------------------------------------------------------- |
|  5   | + `VLLM_MAX_NUM_BATCHED_TOKENS=3072`           | Chunk size 3072 (giữa 2048 và 4096) — tìm điểm sweet-spot chi tiết hơn.    |
|  6   | + `VLLM_MAX_NUM_BATCHED_TOKENS=5120`           | Chunk size 5120 — kiểm tra xem prefill lớn hơn 4096 có hiệu quả hơn không. |
|  7   | + `--block-size=8`                             | Giảm block size KV cache từ 16 (mặc định) xuống 8 → giảm phân mảnh bộ nhớ. |

#### Nhóm 3: N-gram Speculative Decoding (3 slots)

_Mục tiêu: Đánh giá N-gram Speculative trên nền Custom Image._

| Slot | Cấu hình thay đổi so với Baseline Custom Image                                                 | Mục tiêu                                                                      |
| :--: | :--------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------- |
|  8   | + `--speculative-model [ngram]` + `--ngram-prompt-lookup-max=4` + `--num-speculative-tokens=3` | N-gram 4-token window, sinh 3 token speculative → đánh giá tác động lên TPOT. |
|  9   | + `--speculative-model [ngram]` + `--ngram-prompt-lookup-max=6` + `--num-speculative-tokens=5` | N-gram 6-token window, sinh 5 token speculative → mức aggressive hơn.         |
|  10  | + `--speculative-model [ngram]` + `--ngram-prompt-lookup-max=3` + `--num-speculative-tokens=2` | N-gram 3-token window, sinh 2 token speculative → mức conservative.           |

#### Nhóm 4: Hardware & Concurrency Tuning (5 slots)

_Mục tiêu: Đánh giá tác động của Triton custom kernels, tối ưu hóa cơ chế async output và hàng đợi đồng thời._

| Slot | Cấu hình thay đổi so với Baseline Custom Image | Mục tiêu                                                                                                       |
| :--: | :--------------------------------------------- | :------------------------------------------------------------------------------------------------------------- |
|  11  | `VLLM_CUSTOM_KERNEL=0`                         | Tắt Triton kernels (giữ Warmup) để đo trực tiếp chênh lệch tốc độ của base stock layer (không custom kernels). |
|  12  | + `--disable-async-output-proc`                | Tắt xử lý output không đồng bộ để giảm CPU scheduling jitter trên host 3 cores.                                |
|  13  | + `VLLM_MAX_NUM_SEQS=48`                       | Tăng concurrency lên 48 để giải phóng hàng đợi prefill.                                                        |
|  14  | + `VLLM_MAX_NUM_SEQS=24`                       | Giảm concurrency xuống 24 để tối ưu hóa TPOT của bước decode.                                                  |
|  15  | Combo tối ưu nhất                              | Ghép combo các nhân tố tốt nhất tìm được từ Slot 1-14.                                                         |

---

## 3. LỊCH TRÌNH NGÀY 18/07/2026

> [!TIP]
> Nhờ hạ tầng Ghost Strategy v4-v11 từ Round 1 (hijack scripts, Dockerfile pipeline, Docker Hub), việc build custom image cho LFM2.5 có thể hoàn thành trong **nửa ngày** thay vì 2-3 ngày.

### Sáng (7:00 - 12:00): Build Custom Image & Nộp Nhóm G1-G2

| Giờ         | Hành động                                                                                 |
| :---------- | :---------------------------------------------------------------------------------------- |
| 07:00-08:00 | Adapt hijack script v11 cho LFM2.5 (thay entrypoint, env vars).                           |
| 08:00-09:30 | Build custom image `ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1` + push Docker Hub. |
| 09:30-11:00 | Nộp Nhóm G1 (Baseline & Compilation, 4 slots).                                            |
| 11:00-12:00 | Nộp Nhóm G2 (Scheduler, 3 slots).                                                         |

### Chiều (12:00 - 19:00): Nộp Nhóm G3 + Nhóm G4

| Giờ         | Hành động                                                                              |
| :---------- | :------------------------------------------------------------------------------------- |
| 12:00-13:30 | Nộp Nhóm G3 (N-gram Speculative, 3 slots) và thu thập kết quả G1-G2.                   |
| 13:30-15:00 | Nộp Nhóm G4 (Hardware/Concurrency, 5 slots) và theo dõi kết quả.                       |
| 15:00-17:00 | Phân tích toàn bộ dữ liệu 14 slots đầu tiên để chuẩn bị cấu hình tốt nhất cho slot 15. |
| 17:00-19:00 | Tinh chỉnh cấu hình và nộp slot 15 (Combo tối ưu nhất).                                |

### Tối (19:00 - 22:00): Phân tích & Đánh giá

| Giờ         | Hành động                                                              |
| :---------- | :--------------------------------------------------------------------- |
| 19:00-20:00 | Tổng kết kết quả toàn bộ 15 slots hôm nay, cập nhật logs-round2.md.    |
| 20:00-22:00 | Đánh giá độ ổn định của Custom Image, đề xuất kế hoạch cho ngày 19/07. |

---

## 4. PHÂN BỔ 15 SLOTS NGÀY 18/07

|  Nhóm  |  Slots  | Số lượng | Nội dung                                       |  Ưu tiên   |
| :----: | :-----: | :------: | :--------------------------------------------- | :--------: |
| **G1** |  1 - 4  |    4     | Custom Image Baseline & Compilation            | ⭐ Rất cao |
| **G2** |  5 - 7  |    3     | Scheduler & Chunk Size Fine-tuning             |    Cao     |
| **G3** | 8 - 10  |    3     | N-gram Speculative Decoding                    |    Cao     |
| **G4** | 11 - 15 |    5     | Hardware & Concurrency Tuning (Combo tốt nhất) | ⭐ Rất cao |

---

## 5. NEW GOLDEN BASELINE CONFIG (Cập nhật sau Nhóm 1 & Nhóm 2)

Sau kết quả của Nhóm 1 và Nhóm 2, cấu hình baseline tối ưu nhất hiện nay chuyển từ nền Eager sang nền **Compile Level 3** (không bật Chunked Prefill):

```yaml
services:
  model:
    image: ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1
    environment:
      - OMP_NUM_THREADS=2
      - VLLM_WARMUP=1
      - VLLM_CUSTOM_KERNEL=1
      - VLLM_MAX_NUM_SEQS=32
      - VLLM_QUANTIZATION=fp8
    entrypoint:
      - python3
      - -m
      - vllm.entrypoints.openai.api_server
    command:
      - --model=/model # Giữ nguyên model gốc theo đúng quy định BTC
      - --served-model-name=LFM2.5-1.2B-Instruct
      - --host=0.0.0.0
      - --port=8000
      - --max-model-len=8192
      - --gpu-memory-utilization=0.95
      - --tensor-parallel-size=1
      - --enable-prefix-caching
      - --no-enable-log-requests
      - --disable-log-stats
      - --compilation-config={"level":3} # Bật Compile Level 3 mặc định
    ports:
      - "8000:8000"
    shm_size: "2g"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
```

---

## 6. ĐÁNH GIÁ RỦI RO & PHƯƠNG ÁN DỰ PHÒNG

| Rủi ro                                                 | Xác suất | Tác động | Phương án dự phòng                                                                |
| :----------------------------------------------------- | :------: | :------: | :-------------------------------------------------------------------------------- |
| Custom kernels bị lỗi logic làm giảm độ chính xác GPQA |    TB    |   Cao    | Bật debug logs và verify accuracy offline trước khi chọn làm submissions chốt.    |
| N-gram speculative gây crash/lỗi trên LFM2.5           |    TB    |    TB    | Bỏ Nhóm G3, chuyển 3 slot sang Nhóm G4.                                           |
| Compilation level 3 gây lỗi khởi động (timeout)        |    TB    |   Thấp   | Fallback sang level 2 hoặc bỏ qua.                                                |
| Custom Image không cải thiện so với Stock Image        |    TB    |    TB    | Tiếp tục tối ưu custom kernel hoặc chuyển sang test offline speculative decoding. |

> [!NOTE]
> **Mục tiêu ngày 18/07 (Giai đoạn 2 của lộ trình tổng thể):**
>
> - **Mục tiêu tối thiểu**: Xác nhận Custom Image baseline đạt độ ổn định và không suy giảm điểm so với Stock Image (kỳ vọng mốc **58.94**).
> - **Mục tiêu trung bình**: Kỷ lục mới đã thiết lập trên Custom Image + Compile Level 3 là **60.91** (vượt mốc stock baseline 58.94).
> - **Mục tiêu lý tưởng mới**: Custom Image + compile L3 + spec-dec tạo đột phá lớn (**61.5-64+ điểm**).

---

- [x] Build và push Custom Image `ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1` (Đã hoàn thành)
- [x] Chuẩn bị 15 file docker-compose cho các slots thử nghiệm. (Đã hoàn thành)
- [x] Nộp Nhóm G1 (Baseline & Compilation, 4 slots) và thu thập kết quả. (Đã hoàn thành)
- [x] Nộp Nhóm G2 (Scheduler, 3 slots) và thu thập kết quả. (Đã hoàn thành)
- [x] Nộp Nhóm G3 (N-gram, 3 slots) và thu thập kết quả. (Đã hoàn thành)
- [/] Nộp Nhóm G4 (Hardware/Concurrency + Combo, 5 slots) và thu thập kết quả. (Đang thực hiện, Slot 11 đạt kết quả)
- [ ] Tổng kết ngày, cập nhật logs-round2.md và thiết lập baseline mới.

---

## 6. NHẬT KÝ THỰC THI & KẾT QUẢ THỰC TẾ (ACTUAL RUN LOG)

Dưới đây là ghi nhận kết quả thực tế của các slots sau khi nộp lên hệ thống chấm điểm của BTC:

|  STT   | Slot | Mã / Thư mục nộp | Cấu hình thử nghiệm                                    | Kết quả ERS | TTFT P50 | TTFT P95 | TPOT | Kết quả chi tiết & Phân tích                                                                                                   |
| :----: | :--: | :--------------- | :----------------------------------------------------- | :---------: | :------: | :------: | :--: | :----------------------------------------------------------------------------------------------------------------------------- |
| **31** |  1   | `0853`           | Custom Image Base (OMP=2, FP8, Warmup, Custom Kernels) |  **59.51**  |   50ms   |   86ms   | 4ms  | **KỶ LỤC MỚI 59.51**. Warmup JIT tối ưu hóa TTFT P50 cực tốt (50ms). Triton recurrent kernels chạy chính xác và ổn định.       |
| **32** |  2   | `0857`           | Custom Image + compile level=3                         |  **60.91**  |   45ms   |   70ms   | 4ms  | **KỶ LỤC MỚI 60.91**. Bật compile level 3 giúp TTFT P50/P95 cải thiện vượt trội (45ms/70ms) nhờ tối ưu hóa đồ thị CUDA.        |
| **33** |  3   | `0912`           | Custom Image + compile level=2                         | **FAILED**  |    -     |    -     |  -   | **THẤT BẠI (TIMEOUT)**. Job bị treo hoặc timeout khởi chạy vượt quá 2700s, xác nhận compile level 2 không ổn định.             |
| **34** |  4   | `0939`           | Custom Image + enforce-eager                           |  **57.06**  |   62ms   |   89ms   | 4ms  | Tắt CUDA Graphs làm tăng mạnh TTFT P50 (62ms) và giảm điểm còn 57.06, khẳng định vai trò cực kỳ quan trọng của CUDA Graphs.    |
| **35** |  5   | `1008`           | Custom Image + Chunk=3072                              |  **60.31**  |   50ms   |   73ms   | 4ms  | Chunked Prefill 3072 làm tăng TTFT P50 từ 45ms lên 50ms so với Slot 2, làm giảm nhẹ ERS xuống 60.31.                           |
| **36** |  6   | `1010`           | Custom Image + Chunk=5120                              |  **59.80**  |   55ms   |   73ms   | 4ms  | Chunked Prefill 5120 giảm failed xuống còn 4 requests (thấp nhất) nhưng làm tăng TTFT P50 lên 55ms, kéo điểm xuống 59.80.      |
| **37** |  7   | `1013`           | Custom Image + block-size=8                            |  **59.76**  |   52ms   |   80ms   | 4ms  | block-size=8 giúp giảm failed xuống 5 requests nhưng làm tăng overhead quản lý khối, khiến TTFT P50 tăng lên 52ms.             |
| **38** |  8   | `1031`           | Custom Image + Spec N-gram (4, 3)                      |  **59.80**  |   51ms   |   82ms   | 4ms  | Speculative N-gram (4, 3) làm tăng TTFT P50 lên 51ms và không hạ được TPOT, do overhead xác thực lớn trên mô hình nhỏ.         |
| **39** |  9   | `1037`           | Custom Image + Spec N-gram (6, 5)                      |  **60.00**  |   51ms   |   75ms   | 4ms  | Speculative N-gram (6, 5) kéo TTFT P95 xuống 75ms so với Slot 8 nhờ tăng tỷ lệ chấp nhận, nhưng P50 vẫn ở mức 51ms.            |
| **40** |  10  | `1326`           | Custom Image + Spec N-gram (3, 2)                      |  **60.46**  |   51ms   |   72ms   | 4ms  | Speculative N-gram (3, 2) nhẹ hơn giảm bớt overhead giúp kéo ERS lên 60.46 và P95 xuống 72ms, nhưng vẫn kém Slot 2 (60.91).    |
| **41** |  11  | `1340`           | Custom Image + Custom Kernel=0                         |  **56.93**  |   64ms   |   88ms   | 4ms  | Tắt Custom Triton Kernels làm TTFT P50 tăng vọt từ 45ms lên 64ms và giảm ERS mất 3.98 điểm, chứng minh Triton Kernels cực tốt. |

---

## 7. ĐÁNH GIÁ NHÓM 1 & CẢI TIẾN CHIẾN LƯỢC CÁC SLOTS TIẾP THEO

Qua kết quả thực tế từ Nhóm 1 (Slot 1 -> Slot 4), chúng ta rút ra những bài học xương máu và tiến hành điều chỉnh chiến lược như sau:

### 1. Phân tích & Đúc rút kết quả Nhóm 1

1. **Custom Image mang lại lợi thế lớn (+0.57 điểm)**:
   - Slot 1 (Custom Image Base) đạt **59.51** so với Stock Baseline STT 16 (**58.94**).
   - Cơ chế **Warmup JIT** hoạt động hoàn hảo, kéo giảm TTFT P50 từ **58ms xuống còn 50ms** (-13.8%) nhờ triệt tiêu hoàn toàn độ trễ khởi tạo/biên dịch của GPU khi nhận request đầu tiên từ Grader.

2. **`torch.compile` level 3 là "mỏ vàng" đột phá (+1.40 điểm)**:
   - Slot 2 (Compile L3) đạt **60.91** (kỷ lục mới đầu tiên vượt mốc 60 điểm).
   - TTFT P50 giảm sâu từ 50ms xuống **45ms**; đặc biệt TTFT P95 giảm cực mạnh từ 86ms xuống **70ms** (-18.6%).
   - CUDA Graphs kết hợp với gộp kernel (operator fusion) aggressive giúp triệt tiêu CPU scheduling overhead khi xử lý hàng đợi dồn dập (Poisson bursts).

3. **Compile level 2 cực kỳ nguy hiểm (TIMEOUT / TREO CONTAINER)**:
   - Slot 3 (Compile L2) bị treo cứng tại startup (vượt quá 2700s của Grader). Lý do là vì việc compile bán phần tạo ra các đồ thị động không đồng nhất gây infinite compile loops hoặc deadlock trong vLLM/PyTorch.
   - **Quyết định**: Cấm hoàn toàn việc sử dụng compile level 2.

4. **CUDA Graphs là bắt buộc (Eager Mode sụt giảm nghiêm trọng)**:
   - Slot 4 (Enforce Eager) sụt điểm xuống còn **57.06**, TTFT P50 vọt lên **62ms**, TTFT P95 vọt lên **89ms**.
   - Điều này xác nhận với mô hình nhỏ như LFM2.5 (1.2B), overhead gọi kernel từ Python là cực kỳ lớn. CUDA Graphs là bắt buộc phải được kích hoạt.

### 2. Cải tiến Chiến lược: Chuyển dịch Baseline lên Nền Compile Level 3

Vì **Compile Level 3** mang lại cải tiến quá lớn cho TTFT mà vẫn giữ được độ ổn định (failed_count = 7 so với 6 của baseline), **chúng ta sẽ nâng cấu hình cơ sở cho tất cả các slots tiếp theo (Slot 5 -> 14) lên nền Compile Level 3** thay vì chạy trên nền Custom Image Baseline thuần.

Cụ thể, các file docker-compose của Slot 5 đến Slot 14 sẽ được cập nhật bổ sung tham số:
`--compilation-config '{"level": 3}'`

Bảng so sánh cấu hình điều chỉnh:

| Slot | Cấu hình cũ (Nền Eager Custom Image) | Cấu hình mới (Nền Compile L3 Custom Image) | Mục đích thử nghiệm sau điều chỉnh                                            |
| :--: | :----------------------------------- | :----------------------------------------- | :---------------------------------------------------------------------------- |
|  5   | Chunk=3072                           | Chunk=3072 + Compile L3                    | Đánh giá Chunked Prefill và kích thước chunk ngọt trên nền tối ưu đồ thị.     |
|  6   | Chunk=5120                           | Chunk=5120 + Compile L3                    | Đánh giá Chunked Prefill lớn hơn trên nền tối ưu đồ thị.                      |
|  7   | block-size=8                         | block-size=8 + Compile L3                  | Giảm block size KV cache để hạn chế phân mảnh bộ nhớ khi compile L3.          |
|  8   | Spec N-gram (4, 3)                   | Spec N-gram (4, 3) + Compile L3            | Kết hợp Speculative Decoding conservative với Compile L3 (Combo rất hứa hẹn). |
|  9   | Spec N-gram (6, 5)                   | Spec N-gram (6, 5) + Compile L3            | Speculative Decoding aggressive kết hợp với Compile L3.                       |
|  10  | Spec N-gram (3, 2)                   | Spec N-gram (3, 2) + Compile L3            | Speculative Decoding nhẹ kết hợp với Compile L3.                              |
|  11  | Custom Kernel = 0                    | Custom Kernel = 0 + Compile L3             | Đo lường tác động riêng biệt của Triton Kernels khi đã bật Compile L3.        |
|  12  | disable-async-output                 | disable-async-output + Compile L3          | Đánh giá tối ưu CPU scheduling jitter khi có Compile L3.                      |
|  13  | Seqs=48                              | Seqs=48 + Compile L3                       | Tăng concurrency lên 48 khi có Compile L3 để tăng throughput mà không nghẽn.  |
|  14  | Seqs=24                              | Seqs=24 + Compile L3                       | Giảm concurrency xuống 24 khi có Compile L3 để tối ưu tối đa TPOT/TTFT.       |
|  15  | Combo tốt nhất                       | Combo tốt nhất                             | Tùy chỉnh theo kết quả tốt nhất thực tế thu được từ Slot 5-14.                |

### 3. Đánh giá Nhóm 2 & Tinh chỉnh Định hướng

Sau khi hoàn thành Nhóm G2 (Slot 5, Slot 6, Slot 7), chúng ta xác nhận các kết luận kỹ thuật sau:

1. **Chunked Prefill làm suy giảm ERS đối với LFM2.5**:
   - Cả chunk size 3072 (Slot 5: 60.31) và 5120 (Slot 6: 59.80) đều cho điểm số thấp hơn mốc compile L3 thuần không chunk (Slot 2: 60.91). Overhead chia nhỏ prefill làm tăng trễ TTFT P50 thêm 5ms - 10ms.
   - **Tác động tích cực**: Sử dụng chunk size lớn (5120) giúp ổn định scheduler và giảm mạnh số request lỗi xuống còn **4** (mức thấp nhất). Tuy nhiên, lợi ích này không đủ bù lại mức tăng trễ của các request thành công.
   - **Định hướng**: Bỏ Chunked Prefill trong cấu hình combo cuối cùng (Slot 15).

2. **block-size=8 làm tăng trễ quản lý block**:
   - Thử nghiệm block-size=8 (Slot 7: 59.76) cho thấy trễ TTFT P50 tăng lên 52ms và TTFT P95 tăng lên 80ms. Việc quản lý quá nhiều block nhỏ làm gia tăng overhead tính toán và truy xuất của vLLM trên 3 CPU cores.
   - **Định hướng**: Duy trì block-size=16 (mặc định) cho cấu hình tối ưu.

3. **Đánh giá Nhóm G3 (Speculative Decoding - Slot 8, 9 & 10)**:
   - Cả 3 slot speculative (Slot 8, 9, 10) đều sụt giảm ERS so với mốc Compile L3 không speculative (Slot 2: 60.91). TTFT P50 bị kẹt cứng ở **51ms** (+13.3% so với 45ms) do overhead lập lịch và chạy đồ thị JIT phức tạp của speculative logic. TPOT Median vẫn không thể giảm xuống dưới **4ms**.
   - **Lý giải nguyên nhân**: LFM2.5 (1.2B) có recurrent layers tính toán quá nhanh (sinh token gốc mất ~2.2ms). Do đó, overhead tìm kiếm chuỗi N-gram (prompt lookup) và xác thực logits trên CPU 3 cores lớn hơn nhiều so với lợi ích sinh song song.
   - **Tương quan giữa các cấu hình Speculative**:
     - **Slot 10 (3-2) đạt kết quả tốt nhất nhóm (60.46)**, tiếp đến là Slot 9 (60.00) và Slot 8 (59.80).
     - Cấu hình rất nhẹ (3-2) giảm thiểu tối đa overhead tính toán nháp và xác thực logits, nhờ đó kéo trễ đuôi TTFT P95 xuống còn **72ms** (so với 82ms của Slot 8) và giảm failed_count xuống còn **5 requests**.
     - Tuy nhiên, ngay cả ở mức nhẹ nhất, nó vẫn kém xa cấu hình gốc không speculative (Slot 2: 60.91).
   - **Định hướng**: Bỏ hoàn toàn Speculative Decoding trong cấu hình combo cuối cùng (Slot 15) cho LFM2.5.

### 4. Đánh giá Nhóm G4 (Hardware/Concurrency - Slot 11)

1. **Custom Triton Kernels là cực kỳ thiết yếu (-3.98 điểm nếu tắt)**:
   - Thử nghiệm tắt Triton Kernels (Slot 11: 56.93) cho thấy hiệu năng sụt giảm nghiêm trọng so với mốc Compile L3 có bật Triton Kernels (Slot 2: 60.91).
   - Trễ TTFT P50 tăng vọt từ 45ms lên **64ms (+42.2%)** và TTFT P95 tăng vọt từ 70ms lên **88ms (+25.7%)**.
   - **Lý giải nguyên nhân**: Triton Kernels thực hiện operator fusion tối ưu cực tốt cho các cụm tuần hoàn (recurrent/linear layers) đặc thù của LFM2.5, giúp giảm đáng kể số lần đọc weights và overhead truyền băng thông bộ nhớ VRAM. Khi biên dịch với `torch.compile` L3, sự thiếu vắng các kernel tối ưu này làm cho đồ thị CUDA compile tạo ra nhiều kernel rời rạc hơn, gây thâm hụt hiệu năng nặng nề.
   - **Định hướng**: Bắt buộc phải duy trì `VLLM_CUSTOM_KERNEL=1` (mặc định của Custom Image) trong mọi cấu hình nộp bài và combo tối ưu cuối cùng.

---

## 8. PHÂN TÍCH CHIẾN LƯỢC: TẠI SAO ĐIỂM SỐ BỊ KẸT Ở ~61 VÀ LÀM THẾ NÀO ĐỂ BỨT PHÁ

> [!IMPORTANT]
> Sau 11 thử nghiệm (Slot 1→11), **mọi tối ưu hóa ở cấp cờ vLLM (flag-level) đã bão hòa**. Tất cả các "add-on" (Chunked Prefill, Speculative Decoding, block-size, tắt async output) đều **làm giảm** ERS so với mốc Compile L3 thuần (60.91). Để bứt phá, cần phương pháp tiếp cận hoàn toàn khác.

### 8.1. Phân tích Độ nhạy Điểm số (Score Sensitivity Analysis)

**Công thức tính điểm (per-request):**

```
S_request = 0.5 × s_ttft + 0.5 × s_tpot
s_ttft = clamp((400 - TTFT) / 390, 0, 1)²
s_tpot = clamp((10 - TPOT_mean) / 9, 0, 1)²
```

**Hiện trạng kỷ lục (Slot 2: 60.91 điểm):**

| Chỉ số             | Giá trị |  s_score  | Đóng góp (×0.5) | Nhận xét                     |
| :----------------- | :-----: | :-------: | :-------------: | :--------------------------- |
| TTFT (P50=45ms)    |  45 ms  |   0.829   |      41.5       | **Đã tối ưu tốt** (gần trần) |
| TPOT (Median)      |  4 ms   |   0.444   |      22.2       | **CỔ CHAI CHÍNH** (rất thấp) |
| **Tổng lý thuyết** |         | **0.637** |    **63.7**     | Lý thuyết nếu 0 failed       |
| Failed (7/420)     |    7    |     -     |      -2.8       | Mất ~1 điểm/3 failed req     |
| **ERS thực tế**    |         |           |    **60.91**    |                              |

### 8.2. Bảng Kịch bản Bứt phá

| Kịch bản             | TPOT | s_tpot | TTFT P50 | s_ttft | ERS  | Δ so 60.91 | Phương pháp cần                        |
| :------------------- | :--: | :----: | :------: | :----: | :--: | :--------: | :------------------------------------- |
| Hiện tại (Slot 2)    | 4ms  | 0.444  |   45ms   | 0.829  | 63.7 |     —      | (baseline)                             |
| **Giảm TPOT → 3ms**  | 3ms  | 0.605  |   45ms   | 0.829  | 71.7 |  **+8.0**  | INT4 weights / Draft Model             |
| **Giảm TPOT → 2ms**  | 2ms  | 0.790  |   45ms   | 0.829  | 81.0 | **+17.3**  | INT4 + Flash-Linear-Attention          |
| Giảm TTFT → 30ms     | 4ms  | 0.444  |   30ms   | 0.860  | 65.2 |    +1.5    | Warmup shape-match / CUDA graph tuning |
| TPOT 3ms + TTFT 30ms | 3ms  | 0.605  |   30ms   | 0.860  | 73.3 |    +9.6    | INT4 + Warmup tuning                   |
| TPOT 2ms + TTFT 25ms | 2ms  | 0.790  |   25ms   | 0.872  | 83.1 |   +19.4    | Full custom pipeline                   |

> [!CAUTION]
> **Phát hiện cốt lõi**: TPOT là đòn bẩy lớn nhất. Giảm TPOT 1ms (từ 4→3ms) mang lại **+8 điểm**, trong khi giảm TTFT 15ms (từ 45→30ms) chỉ mang lại **+1.5 điểm**. Toàn bộ nỗ lực ngày 18/07 (flag tuning) chỉ tối ưu được TTFT nhưng TPOT vẫn kẹt cứng ở 4ms.

### 8.3. Nguyên nhân Gốc: Tại sao TPOT bị kẹt ở 4ms

```
Breakdown TPOT = 4ms (FP8, Batch=32):
├─ Weight reads (FP8, 1.2GB)       ~1.75 ms  (44%) ← KHÔNG THỂ GIẢM bằng flags
├─ KV cache reads (attention)       ~0.60 ms  (15%) ← Phụ thuộc context length
├─ Recurrent state update           ~0.40 ms  (10%) ← Cố định theo kiến trúc
├─ Computation (MatMul + Act)       ~0.50 ms  (13%) ← Đã tối ưu bởi Triton kernels
└─ Overhead (kernel launch, sched)  ~0.75 ms  (18%) ← Đã tối ưu bởi Compile L3
```

**Kết luận**: Các tối ưu flag-level (Compile L3, CUDA Graphs, Triton Kernels) đã ép **Overhead** từ ~1.2ms xuống ~0.75ms, nhưng **Weight reads (1.75ms)** là thành phần cố định, phụ thuộc hoàn toàn vào:

- Kích thước weights (FP8 = 1 byte/param → 1.2GB)
- Băng thông VRAM (H200 MiG ~500 GB/s)

Không có cờ nào của vLLM có thể giảm weight reads. Cần **giảm kích thước weights** hoặc **giảm số lần đọc weights per token**.

### 8.4. Cập nhật Cấu hình Slot 15 (Golden Combo)

Dựa trên bằng chứng từ 11 slots, **Slot 15 đã được cập nhật để LOẠI BỎ Speculative Decoding** và chạy cấu hình Compile L3 thuần:

```yaml
# 15-docker-compose.yml — Golden Combo (giống hệt Slot 2)
services:
  model:
    image: ptquanh/sandbox-runtime:vllm-lfm25-fp8-kernels-v1
    environment:
      - OMP_NUM_THREADS=2
      - VLLM_WARMUP=1
      - VLLM_CUSTOM_KERNEL=1
      - VLLM_MAX_NUM_SEQS=32
      - VLLM_QUANTIZATION=fp8
    command:
      - --model=/model
      - --max-model-len=8192
      - --compilation-config={"level":3}
      - --enable-prefix-caching
      # KHÔNG CÓ spec-dec, KHÔNG CÓ chunked prefill, KHÔNG CÓ block-size=8
```

> Mục đích: Nộp lại cấu hình Slot 2 để xác nhận tính ổn định và khả năng tái tạo kết quả 60.91. Nếu may mắn có variance thuận, có thể đạt 61-62.

### 8.5. Chiến lược Bứt phá cho Ngày 19-21/07 (TPOT ≤ 3ms → 65-72 điểm)

#### Chiến lược A: INT4 Weight Quantization Online ⭐⭐⭐ (ƯU TIÊN #1)

**Mục tiêu**: Giảm weight reads từ 1.75ms → ~0.87ms → TPOT ~3.1ms → **+8 điểm**.

- **Phương pháp**: Sử dụng `--quantization=compressed-tensors` hoặc `--quantization=marlin` của vLLM để lượng tử hóa online weights FP8 → INT4 ngay tại thời điểm nạp model.
- **Cần nghiên cứu**:
  1. vLLM v0.22.1 có hỗ trợ INT4 online quantization cho LFM2.5 không?
  2. Nếu không, cần build Custom Image v2 với `auto-gptq` hoặc `bitsandbytes` tích hợp.
  3. Kiểm tra accuracy_drop — INT4 có thể làm suy giảm chất lượng output, gây penalty f_delta.
- **Rủi ro**: Cao (chưa xác nhận tương thích kiến trúc hybrid recurrent).
- **Kỳ vọng nếu thành công**: **68-72 điểm**.

#### Chiến lược B: LFM2.5-350M Draft Model Speculative Decoding ⭐⭐ (ƯU TIÊN #2)

**Mục tiêu**: Giảm số forward pass của model lớn → TPOT hiệu quả ~2-3ms → **+8-12 điểm**.

- **Phương pháp**: Đóng gói LFM2.5-350M (cùng họ kiến trúc) sẵn trong Docker Image v2 tại đường dẫn `/draft_model`. Cờ khởi chạy:
  ```yaml
  command:
    - --model=/model
    - --speculative-model=/draft_model
    - --num-speculative-tokens=5
    - --speculative-draft-tensor-parallel-size=1
    - --compilation-config={"level":3}
  ```
- **Tại sao khác N-gram**: N-gram speculative chạy trên CPU (tìm kiếm chuỗi), overhead cố định lớn. Draft model chạy trên GPU, tạo draft tokens nhanh hơn nhiều và có tỷ lệ chấp nhận cao hơn do cùng họ kiến trúc.
- **Cần nghiên cứu**:
  1. Tải và kiểm tra LFM2.5-350M tương thích speculative decoding của vLLM.
  2. Kích thước image Docker (cần gói thêm ~350MB weights).
  3. VRAM sharing giữa model chính và draft model trên MiG partition.
- **Rủi ro**: Trung bình (cần xác nhận vLLM hỗ trợ speculative với kiến trúc hybrid).
- **Kỳ vọng nếu thành công**: **70-78 điểm**.

#### Chiến lược C: Custom Image v2 — Deeper Kernel Fusion ⭐ (ƯU TIÊN #3)

**Mục tiêu**: Giảm overhead computation + weight reads thêm 0.3-0.5ms → TPOT ~3.5ms → **+4-5 điểm**.

- **Phương pháp**: Viết Triton kernels fusion sâu hơn cho toàn bộ pipeline decode:
  1. **Fused Weight Dequant + MatMul**: Kết hợp FP8→FP16 dequant trực tiếp vào kernel GEMM, loại bỏ intermediate buffer.
  2. **Fused Recurrent + Attention**: Gộp tính toán recurrent state update và attention lookup vào 1 kernel.
  3. **Shape-aware Warmup**: Pre-compile CUDA graphs cho đúng batch sizes 24/32/48 và sequence lengths 200/1000/2000/4000 khớp trace.
- **Rủi ro**: Thấp-trung bình (mở rộng từ Triton Kernels đã có).
- **Kỳ vọng**: **64-67 điểm**.

#### Chiến lược D: Giảm max-model-len + Tối ưu Block Allocator

**Mục tiêu**: Giảm failed_count từ 7 → 0-2, kéo TTFT P50 xuống thêm 2-3ms.

- **Phương pháp**: Trace benchmark chỉ dùng tối đa ~4200 tokens (4000 input + 200 output). Giảm `--max-model-len` từ 32768 xuống **8192** hoặc **16384**:
  - Giải phóng GPU memory → ít preemption → ít failed requests
  - Block table nhỏ hơn → giảm overhead quản lý bộ nhớ
- **Lưu ý**: Đã thất bại 2 lần trên stock image (STT 14 và STT 20), nhưng chưa thử trên Custom Image + Compile L3. Cần thử lại cẩn thận.
- **Kỳ vọng**: **+0.5-1.5 điểm** (bổ trợ).

### 8.6. Lộ trình Cập nhật

| Ngày  | Trọng tâm                                            | Mục tiêu điểm |
| :---: | :--------------------------------------------------- | :-----------: |
| 18/07 | ✅ Hoàn thành flag sweep 15 slots (đang thực hiện)   |     60-62     |
| 19/07 | Nghiên cứu INT4 online quant + Build Custom Image v2 |     62-65     |
| 20/07 | Submit INT4 / Draft Model experiments                |     65-72     |
| 21/07 | Tối ưu Triton kernel fusion + Combo                  |     68-75     |
| 22-25 | Speculative Decoding với LFM2.5-350M                 |     72-82     |
