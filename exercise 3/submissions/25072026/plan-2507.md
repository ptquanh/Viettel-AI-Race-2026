# Kế Hoạch Bứt Phá Tối Đa Điểm Số — Ngày Cuối 25/07/2026

> **Kỷ lục tuyệt đối**: **61.59đ** (STT 118, Image v14 FP8 + `spawn` + `FLASHINFER`, TTFT P50=47ms, P95=68ms)
> **Thử nghiệm Slot 03 (08:14)**: Image v15 (`torchao` INT4) pass 100% không lỗi nhưng bị vLLM fallback sang Eager PyTorch Linear Execution → TTFT P50 bị đẩy lên 74ms, điểm sụt còn 53.78đ.
> **Kết luận Động cơ**: **Image v14 FP8 Native là động cơ tối ưu kịch sàn và ổn định nhất**.
> **Slots còn lại ngày 25/07**: **12 slots**

---

## 1. PHÂN TÍCH THỬ NGHIỆM IMAGE V15 (TORCHAO INT4)

- **Ưu điểm**: Container khởi chạy 100% thành công, 0% Accuracy Drop.
- **Hạn chế**: Vì vLLM v0.6+ chưa có custom Triton/Marlin INT4 GEMM kernel cho LFM Recurrent Layers khi ép quantize qua `torchao`, vLLM phải fallback về Eager PyTorch Linear computation.
- **Hậu quả**: Overhead launch kernel ở Eager mode đẩy TTFT P50 lên **74ms** và giữ TPOT ở **4ms**.
- **Quyết định Chiến lược**: **Rút lui khỏi nhánh INT4 Eager. Tập trung 100% nguồn lực 12 slots còn lại vào Image v14 FP8 Native** — động cơ đã đưa chúng ta lên đỉnh 61.59 điểm!

---

## 2. CHIẾN LƯỢC TỐI ƯU 12 SLOTS CÒN LẠI (25/07)

### Phase 1: Ban Ngày (Slots 04 - 08) — Fine-tuning Micro Configs

Tận dụng các slots ban ngày để thử nghiệm các biến đổi nhỏ (Micro-tuning) trên nền Image v14 FP8:

1. **`VLLM_GPU_MEMORY_UTILIZATION=0.94` vs `0.95`**:
   - Mốc `0.94` từng đạt 61.11đ lúc 20:12 (TTFT P50 48ms).
   - Mốc `0.95` đạt kỷ lục 61.59đ lúc 13:50 (TTFT P50 47ms).
2. **`VLLM_MAX_NUM_SEQS=32` vs `28` vs `36`**: Test độ nhạy concurrency.
3. **`VLLM_CUDAGRAPH_NUM_OF_WARMUPS=5`**: Giữ chuẩn cố định.

### Phase 2: Đêm Khuya (Slots 09 - 15) — All-In Golden Runs

Đoạn từ 21:00 đến 23:59 đêm nay là **khung giờ vàng (Golden Timing)** khi host BTC có ít đội nộp nhất, trễ hàng đợi (Queue Latency) đạt mức kịch sàn:

- Nộp luân phiên 2 cấu hình tốt nhất giải đấu:
  - **Option A (Champion 61.59đ)**: `GPU_MEM=0.95` + `spawn` + `FLASHINFER` + `BLOCK_SIZE=32`
  - **Option B (Runner-up 61.11đ)**: `GPU_MEM=0.94` + `spawn` + `FLASHINFER` + `BLOCK_SIZE=32`
- **Mục tiêu điểm số đêm nay**: Ép TTFT P50 xuống dưới 44ms → **Chinh phục mốc 62.5 - 64+ điểm!**

---

## 3. LỊCH NỘP BÀI CHI TIẾT

|   Slot    | Khung giờ | Cấu hình nộp                         | Mục tiêu               |
| :-------: | :-------: | :----------------------------------- | :--------------------- |
|  **04**   |   10:30   | Image v14 (`GPU_MEM=0.94` + `spawn`) | Mid-day baseline check |
|  **05**   |   12:00   | Image v14 (`GPU_MEM=0.95` + `spawn`) | Noon baseline check    |
|  **06**   |   14:00   | Image v14 (`GPU_MEM=0.94` + `OMP=1`) | Afternoon test         |
|  **07**   |   16:00   | Image v14 (`GPU_MEM=0.95` + `OMP=1`) | Afternoon test         |
|  **08**   |   18:00   | Best Candidate                       | Evening test           |
|  **09**   |   20:30   | 🌙 Golden Run 1 (`GPU_MEM=0.95`)     | Night timing 1         |
|  **10**   |   21:30   | 🌙 Golden Run 2 (`GPU_MEM=0.94`)     | Night timing 2         |
|  **11**   |   22:30   | 🌙 Golden Run 3 (`GPU_MEM=0.95`)     | Mid-night low noise    |
|  **12**   |   23:15   | 🌙 Golden Run 4 (`GPU_MEM=0.94`)     | Mid-night lowest noise |
|  **13**   |   23:40   | 🌙 Golden Run 5 (`GPU_MEM=0.95`)     | Golden push            |
| **14/15** |   23:55   | 🌙 **CHỐT SỔ CUỐI CÙNG**             | Max score push         |
