# Kế Hoạch Bứt Phá Tối Đa Điểm Số (Đạt 70 Điểm) — Ngày Cuối 25/07/2026

> **Kỷ lục tuyệt đối**: **62.67đ** (STT 130, Image v14 FP8 + `spawn` + `FLASHINFER` + `UVLOOP=1`, TTFT P50=45ms, P95=61ms)
> **Thử nghiệm Slot 03 (08:14)**: Image v15 (`torchao` INT4) pass 100% không lỗi nhưng bị vLLM fallback sang Eager PyTorch Linear Execution → TTFT P50 bị đẩy lên 74ms, điểm sụt còn 53.78đ.
> **Kết luận Động cơ**: **Image v14 FP8 Native là động cơ tối ưu kịch sàn và ổn định nhất**.
> **Mục tiêu**: **70.0+ điểm** trong 12 slots còn lại ngày 25/07.

---

## 1. BÀI TOÁN 70 ĐIỂM (THE 70-POINT BOTTLENECK)

Để đạt 70 điểm, chúng ta cần:

- **TPOT**: Giữ nguyên ở 4ms (mức vật lý tối đa của FP8).
- **TTFT P50/P95**: Kéo xuống dưới 45ms/65ms.
- **Failed Requests**: Ép về **0** (hiện tại thường rớt 4-7 requests do JIT Warmup đụng độ Load Balancer).

### Các chiến thuật tối tân (Advanced Micro-tweaks):

1. **`VLLM_USE_UVLOOP=1`**: Tăng tốc event loop của asyncio trên vLLM API Server. `uvloop` được viết bằng Cython, nhanh gấp 2-4 lần `asyncio` thuần, giúp giảm độ trễ TTFT ở cấp độ networking/scheduling.
2. **`VLLM_LOGGING_LEVEL=ERROR`**: Tắt toàn bộ log `INFO` của vLLM và uvicorn để loại bỏ overhead I/O blocking trong lúc xử lý concurrency cao.
3. **`FLASHINFER_WORKSPACE_SIZE`**: Phân bổ sẵn workspace lớn hơn (ví dụ: `33554432` - 32MB) để tránh cấp phát động VRAM cho FlashInfer trong lúc runtime.
4. **Golden Timing (23:15 - 23:55)**: Yếu tố sống còn. Host grader BTC sẽ có trễ mạng (Noise) bằng 0 vào giờ chót.

---

## 2. CHIẾN LƯỢC TỐI ƯU 12 SLOTS CÒN LẠI (25/07)

### Phase 1: Ban Ngày (Slots 04 - 08) — UVLoop & No-Logging Tests

Tận dụng các slots ban ngày để xác thực tính ổn định của các cờ mới:

- **Slot 04 (10:30)**: Best v14 + `VLLM_USE_UVLOOP=1` + `VLLM_LOGGING_LEVEL=ERROR`.
- **Slot 05 (12:00)**: Image v14 + `UVLOOP=1` + `LOGGING=ERROR` + `FLASHINFER_WORKSPACE=32MB` + `GPU_MEM=0.95`.
- **Slot 06 (14:00)**: Best v14 + `VLLM_MAX_NUM_SEQS=28` (Thử thu hẹp số luồng cùng với uvloop).
- **Slot 07 (16:00)**: Kế thừa best config từ Slot 04/05.
- **Slot 08 (18:00)**: Kế thừa best config từ Slot 04/05.

### Phase 2: Đêm Khuya (Slots 09 - 15) — All-In Golden Runs (Chiến dịch 70 điểm)

Đoạn từ 21:00 đến 23:55 là **khung giờ vàng (Golden Timing)**:

- Nộp luân phiên cấu hình `Image v14 FP8 + spawn + FLASHINFER + UVLOOP=1 + LOGGING=ERROR`.
- **Mục tiêu**: Vượt rào 44ms TTFT và 0 Lỗi.

---

## 3. LỊCH NỘP BÀI CHI TIẾT

|   Slot    | Khung giờ | Cấu hình nộp                                                 | Mục tiêu                         |
| :-------: | :-------: | :----------------------------------------------------------- | :------------------------------- |
|  **04**   |   10:12   | Image v14 FP8 (`GPU_MEM=0.94` + `spawn`)                     | ✅ **62.67đ (KỶ LỤC TOÀN GIẢI)** |
|  **05**   |   12:00   | Image v14 (`UVLOOP=1` + `ERROR` + `WORKSPACE=32MB` + `0.95`) | Ultra-low overhead combo test    |
|  **06**   |   14:00   | Image v14 (`UVLOOP=1` + `VLLM_MAX_NUM_SEQS=28`)              | Concurrency check                |
|  **07**   |   16:00   | Image v14 (Best Candidate Option A)                          | Afternoon test                   |
|  **08**   |   18:00   | Image v14 (Best Candidate Option B)                          | Evening test                     |
|  **09**   |   20:30   | 🌙 Golden Run 1 (`UVLOOP=1` + `spawn` + `GPU_MEM=0.95`)      | Night timing 1                   |
|  **10**   |   21:30   | 🌙 Golden Run 2 (`UVLOOP=1` + `spawn` + `GPU_MEM=0.94`)      | Night timing 2                   |
|  **11**   |   22:30   | 🌙 Golden Run 3 (`UVLOOP=1` + `spawn` + `GPU_MEM=0.95`)      | Mid-night low noise              |
|  **12**   |   23:15   | 🌙 Golden Run 4 (`UVLOOP=1` + `spawn` + `GPU_MEM=0.94`)      | Mid-night lowest noise           |
|  **13**   |   23:40   | 🌙 Golden Run 5 (`UVLOOP=1` + `spawn` + `GPU_MEM=0.95`)      | Golden push                      |
| **14/15** |   23:55   | 🌙 **CHỐT SỔ CUỐI CÙNG (70 POINT PUSH)**                     | Max score push                   |
